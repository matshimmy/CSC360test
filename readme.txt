COMPILE:

run the command
	gcc PMan.c

This will complie all the code.
To run it simply run the a.out file

NOT FINISHED:

The code is not finished, I struggled getting through the first functions.
The bg command works and some of the functions are partially implemented.

